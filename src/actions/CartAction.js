export const ADD = (item) => ({
  type: "ADD",
  product: item,
});
export const REMOVE = (item) => ({
  type: "REMOVE",
  product: item,
});
export const EDIT = (item) => ({
  type: "EDIT",
  product: item,
});
